[package]
name = "vm"
version = "0.1.0"
edition = "2021"
authors = ["Harsh Bajpai"]
license = "MIT"
description = "Virtual machine implementation for Fastpay"

[dependencies]
thiserror = "1.0"
anyhow = "1.0"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
